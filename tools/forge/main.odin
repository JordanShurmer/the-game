package main

/*
forge — Slice 1

Graphical Preview needs vendor:raylib (toolchain check).
Until then the vertical path is proven by:

  odin test src

That loads biomes, generates a solid region, and writes preview.ppm.
*/

import "core:fmt"

main :: proc() {
	fmt.println("Slice 1 forge placeholder")
	fmt.println("Run from repo root:  odin test src")
	fmt.println("Open preview.ppm after the write_preview test.")
	fmt.println("Graphical Preview arrives once raylib is verified.")
}
